export const sendMessage = (message) => {
  // Logic to send the message to the frontend
  console.log(message,"show message");
  // You can use WebSocket, push notifications, or any other method to send the message to the frontend
};